# Build Notes

Build everything with:

```sh
make
```

Run validation with:

```sh
make test
```

The classical scanner, source alignment, evidence publisher, and dashboard are
C++/shell. Python is used for lightweight research validation and README/schema
checks.
